Algoritmo "Atividade2"

Var
contador, numeroDaTabuada : inteiro

Inicio
escreval ("insira a tabuada desejada")
leia (numeroDaTabuada)

contador <- 1
enquanto (contador <= 10) faca

escreval (contador,"x", numeroDaTabuada," = ", contador * numeroDaTabuada)

contador <- contador + 1
fimEnquanto

Fimalgoritmo